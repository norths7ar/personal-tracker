# OpenAI-compatible LLM API key
LLM_API_KEY=your_llm_api_key_here

# App access control. Local development usually keeps this disabled.
AUTH_ENABLED=false
APP_PASSWORD=
APP_SESSION_SECRET=
COOKIE_SECURE=false

# Database backend: sqlite or postgres
DB_BACKEND=sqlite

# Optional SQLite file path. Relative paths are resolved from the project root.
# Docker Compose overrides this to /app/data/expenses.db.
DATABASE_PATH=data/expenses.db

# Docker Compose settings. Use an absolute APP_DATA_DIR on the NAS.
APP_PORT=8080
APP_DATA_DIR=./data

# Option A: full PostgreSQL connection URL.
DATABASE_URL=

# Option B: build a Supabase pooler URL from parts. Useful for local .env files.
SUPABASE_PROJECT_REF=your_project_ref
SUPABASE_PROJECT_PASSWORD=your_supabase_database_password
SUPABASE_POOLER_HOST=aws-0-ap-northeast-1.pooler.supabase.com
